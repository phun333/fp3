# PostgreSQL bağlantı URL'i (tag cache için)
DATABASE_URL="postgresql://postgres:<parola>@localhost:5432/fp3"

# Hangi origin'lerden istek kabul edilsin (Fastify API + web)
CORS_ORIGINS="http://localhost:3001,http://localhost:3000"

# AI servisinin port'u (FP3 standardı: 3002)
PORT=3002
